import type { Config } from 'vike/types'

export default {
  title: "Search · Speedrun Archive Club",
  description: "Search for archived metadata and videos of Twitch broadcasts",
} satisfies Config
